/**
 * SamarthaaMed — email-server.js
 *
 * This is the entry point Render is configured to run.
 * It starts the full backend server (API config + Orthanc proxy + health check).
 */

require('./server');
